import { connect } from "react-redux";
import AntiNutrientsView from "./index";


const mapStoreToProps = state => ({
});


const mapDispatchToProps = dispatch => ({
});

export default connect(mapStoreToProps, mapDispatchToProps)(AntiNutrientsView);
